# P03 对 rc2 恢复窗口增量的消费确认

日期：2026-09-20。负责人：P03 原任务。评审修订 `i03-review-20260920.2`，候选 `teacher-artifact-handoff/1-rc2`。本次仅确认 rc2 增量，原 [rc1 源侧回执](p03-profile-v1-review.md)原文和绑定保留。

| 组成文件 | SHA-256 |
|---|---|
| Identity `dev/i03/review/profile-v1-rc1.md` | `aa685614b96f0fd21a0eb557c2773a99ed1e9f30ab3ded1e7891545f27917f58` |
| Identity `dev/i03/review/profile-v1-rc2.md` | `af86a5cd6c93f8829660eab7bb3984c4296ceafbcb4bf9505c60ee88000c2f5f` |

**ACK-P02：接受 rc2 的时间基准、窗口截断与清理边界，无剩余窗口起点异议；实现仍待共同冻结、提供方及正式源持久层。** ACK-P01/P03 未变化，不重新签为实现完成。D03 普通目标删除的历史原文政策仍待总控决定；本确认不授予任何材料复制权，不构成正式版本已共同通过或生产启用。

源侧接受以下完整语义：

- `t0` 为 Identity 首次成功持久化操作的时刻；可信 `W=operation_expires_at=t0+86400`，各阶段与恢复始终相同。`R=W+29天` 是恢复授权上界，不另增响应字段。协议整数秒与源内部毫秒在边界换算，不能混比。
- 本地正文截止 L 可更早，release 必须满足当前事实有效且 `now<min(L,W)`。写票取 `min(now+120,W)`，status/cancel 票取 `min(now+60,R)`；等于 W/R 即拒绝对应授权，不能由旧 key、换票、锁等待或迟到清理延长。
- 源正文/附件与无正文操作记录分开清理。取得 W 后，恢复记录至少覆盖 R；不得在旧 freeze+30d 提前删回执或取消意图。先查询目标原操作再报告终态，L 已过不能把成功或未知覆盖成 expired。
- 首 issue 响应丢失时保持原 operation/binding，先请求 status 授权恢复可信 W 并查目标；不猜 now+24h，不换操作，不自动重传正文。Identity 暂时 not_prepared 不能证明仍在途的首次尝试不存在，须先确认尝试结束，再按当前 L 决定是否继续原 prepare。
- 未恢复 W 的在途记录不得套用 freeze+30d 自动清理。正式源实现将其与已知期限的记录分开；有界请求与限次恢复仍不确定时，停止自动写入，转为最小故障元数据待对账。对账入口须核实原尝试结束、Identity 历史和目标回执/墓碑，记录依据后才能关闭；不以“查不到”推断未接收。此出口与容量限制须在生产清理实现及验证中交付，本回执没有宣称已实现或自定新的保留期限。
- R 不决定成功资源、采用版本或去重墓碑的删除时间；超过 R 后仍由目标本地 owner 权限访问资源，源不能因旧恢复授权失效而自动新建同选择操作。普通删除和永久去重按 T11 最终政策落实。

核对 rc2 合成算例：freeze 比 t0 早7200秒，L比W早7200秒，旧源元数据截止比R早7200秒；V11 时刻仍有7199秒恢复窗口；W−1/R−1 签出的票分别截到W/R。仅完成文档与整数运算一致性核对，**V10–13 未做端到端执行，不增加三端通过数**。上一轮35项传输/兼容测试不因本文重复计数。

当前 `i03Source.js`/旧 store 的 freeze 起算和 `i03Client.js` 的 draft 白名单保持原样；新 HTTPS 模块仍未装配。正式生产存储需持久 W、独立清理快照/恢复元数据、未决签发对账状态及操作/选择绑定，并补 V10–13、生产权限/撤权串行及重启测试。按既定顺序先正式冻结与提供方，再消费实现和完整应用联验；不静默升级 draft。

此次无运行代码、生产配置、数据库或发布变化。真实教师及手机验收仍待，双站 e4d52d1 整理和选定下载继续保留。T11 主线已接集成责任但尚无新的 ACK-T01–04 或装配完成回执。

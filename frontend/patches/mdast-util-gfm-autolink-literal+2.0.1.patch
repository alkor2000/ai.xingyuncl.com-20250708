diff --git a/node_modules/mdast-util-gfm-autolink-literal/lib/index.js b/node_modules/mdast-util-gfm-autolink-literal/lib/index.js
index c5ca771..d278747 100644
--- a/node_modules/mdast-util-gfm-autolink-literal/lib/index.js
+++ b/node_modules/mdast-util-gfm-autolink-literal/lib/index.js
@@ -132,7 +132,9 @@ function transformGfmAutolinkLiterals(tree) {
     tree,
     [
       [/(https?:\/\/|www(?=\.))([-.\w]+)([^ \t\r\n]*)/gi, findUrl],
-      [/(?<=^|\s|\p{P}|\p{S})([-.\w+]+)@([-\w]+(?:\.[-\w]+)+)/gu, findEmail]
+      // ai-platform patch: no lookbehind (Safari < 16.4 throws SyntaxError at runtime); the
+      // leading character is captured instead and re-emitted as text by `findEmail`.
+      [/(^|[\s\p{P}\p{S}])([-.\w+]+)@([-\w]+(?:\.[-\w]+)+)/gu, findEmail]
     ],
     {ignore: ['link', 'linkReference']}
   )
@@ -194,7 +196,10 @@ function findUrl(_, protocol, domain, path, match) {
  * @param {RegExpMatchObject} match
  * @returns {Link | false}
  */
-function findEmail(_, atext, label, match) {
+function findEmail(_, lead, atext, label, match) {
+  // ai-platform patch: `match.index` points at the captured lead character; shift it so
+  // `previous` still inspects the character right before the address.
+  match = {index: match.index + lead.length, input: match.input}
   if (
     // Not an expected previous character.
     !previous(match, true) ||
@@ -204,12 +209,14 @@ function findEmail(_, atext, label, match) {
     return false
   }
 
-  return {
+  const link = {
     type: 'link',
     title: null,
     url: 'mailto:' + atext + '@' + label,
     children: [{type: 'text', value: atext + '@' + label}]
   }
+  // ai-platform patch: give back the consumed lead character.
+  return lead ? [{type: 'text', value: lead}, link] : link
 }
 
 /**

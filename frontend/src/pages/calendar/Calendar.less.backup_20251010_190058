/**
 * 智能日历 - iOS科技风样式
 * 采用毛玻璃效果、渐变色、柔和阴影
 */

@import '../../styles/theme-variables.css';

// ========== 颜色变量 ==========
@primary-color: #007AFF;
@primary-light: #5AC8FA;
@primary-dark: #0051D5;
@success-color: #34C759;
@warning-color: #FF9500;
@danger-color: #FF3B30;
@info-color: #5856D6;

@bg-primary: rgba(255, 255, 255, 0.85);
@bg-secondary: rgba(242, 242, 247, 0.9);
@bg-card: rgba(255, 255, 255, 0.92);
@bg-hover: rgba(0, 122, 255, 0.08);

@text-primary: #1C1C1E;
@text-secondary: #8E8E93;
@text-tertiary: #C7C7CC;

@border-color: rgba(0, 0, 0, 0.05);
@shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.04);
@shadow-md: 0 4px 16px rgba(0, 0, 0, 0.08);
@shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.12);

// 深色模式颜色
@dark-bg-primary: rgba(28, 28, 30, 0.95);
@dark-bg-secondary: rgba(44, 44, 46, 0.92);
@dark-bg-card: rgba(58, 58, 60, 0.9);
@dark-text-primary: #FFFFFF;
@dark-text-secondary: #EBEBF5;
@dark-border-color: rgba(255, 255, 255, 0.1);

// ========== 主容器 ==========
.calendar-page {
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  padding: 24px;
  
  @media (max-width: 768px) {
    padding: 16px;
  }
}

// ========== 顶部工具栏 ==========
.calendar-header {
  background: @bg-card;
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-radius: 16px;
  padding: 20px 24px;
  margin-bottom: 20px;
  box-shadow: @shadow-md;
  border: 1px solid @border-color;
  
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  
  .header-title {
    h1 {
      margin: 0;
      font-size: 28px;
      font-weight: 700;
      color: @text-primary;
      background: linear-gradient(135deg, @primary-color, @primary-light);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    
    p {
      margin: 4px 0 0;
      font-size: 14px;
      color: @text-secondary;
    }
  }
  
  .header-actions {
    display: flex;
    gap: 12px;
    
    .ant-btn {
      border-radius: 12px;
      border: none;
      height: 40px;
      padding: 0 20px;
      font-weight: 500;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      
      &.ant-btn-primary {
        background: linear-gradient(135deg, @primary-color, @primary-light);
        box-shadow: 0 4px 12px rgba(0, 122, 255, 0.3);
        
        &:hover {
          transform: translateY(-2px);
          box-shadow: 0 6px 16px rgba(0, 122, 255, 0.4);
        }
      }
      
      &.ant-btn-default {
        background: @bg-secondary;
        color: @text-primary;
        
        &:hover {
          background: @bg-hover;
          color: @primary-color;
        }
      }
    }
  }
  
  @media (max-width: 768px) {
    flex-direction: column;
    align-items: stretch;
    
    .header-actions {
      justify-content: stretch;
      
      .ant-btn {
        flex: 1;
      }
    }
  }
}

// ========== 主内容区域 ==========
.calendar-content {
  display: grid;
  grid-template-columns: 300px 1fr 320px;
  gap: 20px;
  align-items: start;
  
  @media (max-width: 1400px) {
    grid-template-columns: 280px 1fr;
    
    .calendar-right-panel {
      display: none;
    }
  }
  
  @media (max-width: 992px) {
    grid-template-columns: 1fr;
  }
}

// ========== 左侧面板（统计概览） ==========
.calendar-left-panel {
  background: @bg-card;
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-radius: 16px;
  padding: 20px;
  box-shadow: @shadow-md;
  border: 1px solid @border-color;
  position: sticky;
  top: 24px;
  
  .overview-section {
    margin-bottom: 24px;
    
    &:last-child {
      margin-bottom: 0;
    }
    
    .section-title {
      font-size: 16px;
      font-weight: 600;
      color: @text-primary;
      margin-bottom: 12px;
      display: flex;
      align-items: center;
      gap: 8px;
      
      .anticon {
        color: @primary-color;
      }
    }
    
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 12px;
      
      .stat-card {
        background: @bg-secondary;
        border-radius: 12px;
        padding: 12px;
        text-align: center;
        transition: all 0.3s ease;
        
        &:hover {
          transform: translateY(-2px);
          box-shadow: @shadow-sm;
        }
        
        .stat-value {
          font-size: 24px;
          font-weight: 700;
          color: @primary-color;
          margin-bottom: 4px;
        }
        
        .stat-label {
          font-size: 12px;
          color: @text-secondary;
        }
        
        &.completed .stat-value { color: @success-color; }
        &.in-progress .stat-value { color: @warning-color; }
        &.not-started .stat-value { color: @text-secondary; }
        &.high-priority .stat-value { color: @danger-color; }
      }
    }
    
    .category-list {
      .category-item {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 10px 12px;
        border-radius: 10px;
        margin-bottom: 8px;
        background: @bg-secondary;
        transition: all 0.2s ease;
        
        &:hover {
          background: @bg-hover;
        }
        
        .category-info {
          display: flex;
          align-items: center;
          gap: 10px;
          
          .category-color {
            width: 12px;
            height: 12px;
            border-radius: 50%;
          }
          
          .category-name {
            font-size: 14px;
            color: @text-primary;
          }
        }
        
        .category-count {
          font-size: 14px;
          font-weight: 600;
          color: @primary-color;
          background: @bg-primary;
          padding: 2px 10px;
          border-radius: 10px;
        }
      }
    }
  }
  
  @media (max-width: 992px) {
    display: none;
  }
}

// ========== 中间面板（日历主体） ==========
.calendar-main-panel {
  background: @bg-card;
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-radius: 16px;
  padding: 24px;
  box-shadow: @shadow-md;
  border: 1px solid @border-color;
  min-height: 600px;
  
  // Ant Design日历自定义样式
  .ant-picker-calendar {
    background: transparent;
    
    .ant-picker-calendar-header {
      padding-bottom: 16px;
      border-bottom: 1px solid @border-color;
      
      .ant-picker-calendar-year-select,
      .ant-picker-calendar-month-select {
        min-width: 100px;
      }
      
      .ant-select-selector {
        border-radius: 10px;
        border-color: @border-color;
        background: @bg-secondary;
        
        &:hover {
          border-color: @primary-color;
        }
      }
    }
    
    .ant-picker-panel {
      background: transparent;
      border: none;
    }
    
    .ant-picker-content {
      thead th {
        color: @text-secondary;
        font-weight: 600;
        font-size: 13px;
        padding: 12px 0;
      }
      
      .ant-picker-cell {
        padding: 4px;
        
        .ant-picker-cell-inner {
          border-radius: 12px;
          height: 80px;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: flex-start;
          padding: 8px 4px;
          transition: all 0.2s ease;
          position: relative;
          
          &:hover {
            background: @bg-hover;
          }
        }
        
        // 日期数字
        .ant-picker-calendar-date-value {
          font-size: 16px;
          font-weight: 500;
          color: @text-primary;
          margin-bottom: 4px;
        }
        
        // 今天高亮
        &.ant-picker-cell-today .ant-picker-cell-inner {
          background: linear-gradient(135deg, rgba(0, 122, 255, 0.1), rgba(90, 200, 250, 0.1));
          border: 2px solid @primary-color;
          
          .ant-picker-calendar-date-value {
            color: @primary-color;
            font-weight: 700;
          }
        }
        
        // 选中日期
        &.ant-picker-cell-selected .ant-picker-cell-inner {
          background: linear-gradient(135deg, @primary-color, @primary-light);
          
          .ant-picker-calendar-date-value {
            color: white;
          }
        }
        
        // 非当前月日期
        &.ant-picker-cell-disabled {
          .ant-picker-calendar-date-value {
            color: @text-tertiary;
          }
        }
        
        // 事项标记点
        .event-dots {
          display: flex;
          gap: 3px;
          margin-top: auto;
          flex-wrap: wrap;
          justify-content: center;
          max-width: 100%;
          
          .event-dot {
            width: 6px;
            height: 6px;
            border-radius: 50%;
            background: @primary-color;
            
            &.high-priority {
              background: @danger-color;
              width: 8px;
              height: 8px;
            }
          }
        }
        
        // 事项数量角标
        .event-count {
          position: absolute;
          top: 6px;
          right: 6px;
          background: @primary-color;
          color: white;
          font-size: 10px;
          font-weight: 600;
          padding: 2px 6px;
          border-radius: 8px;
          min-width: 18px;
          text-align: center;
        }
      }
    }
  }
}

// ========== 右侧面板（事项详情） ==========
.calendar-right-panel {
  background: @bg-card;
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-radius: 16px;
  padding: 20px;
  box-shadow: @shadow-md;
  border: 1px solid @border-color;
  position: sticky;
  top: 24px;
  max-height: calc(100vh - 48px);
  overflow-y: auto;
  
  .panel-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 16px;
    padding-bottom: 12px;
    border-bottom: 1px solid @border-color;
    
    h3 {
      margin: 0;
      font-size: 18px;
      font-weight: 600;
      color: @text-primary;
    }
    
    .date-display {
      font-size: 14px;
      color: @text-secondary;
    }
  }
  
  .events-list {
    .event-card {
      background: @bg-secondary;
      border-radius: 12px;
      padding: 14px;
      margin-bottom: 12px;
      border-left: 4px solid @primary-color;
      transition: all 0.3s ease;
      cursor: pointer;
      
      &:hover {
        transform: translateX(4px);
        box-shadow: @shadow-sm;
      }
      
      .event-header {
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        margin-bottom: 8px;
        
        .event-title {
          font-size: 15px;
          font-weight: 600;
          color: @text-primary;
          flex: 1;
          line-height: 1.4;
        }
        
        .importance-badge {
          padding: 2px 8px;
          border-radius: 8px;
          font-size: 11px;
          font-weight: 600;
          white-space: nowrap;
          margin-left: 8px;
          
          &.high {
            background: rgba(255, 59, 48, 0.1);
            color: @danger-color;
          }
          
          &.medium {
            background: rgba(255, 149, 0, 0.1);
            color: @warning-color;
          }
          
          &.low {
            background: rgba(142, 142, 147, 0.1);
            color: @text-secondary;
          }
        }
      }
      
      .event-meta {
        display: flex;
        align-items: center;
        gap: 12px;
        font-size: 13px;
        color: @text-secondary;
        margin-bottom: 8px;
        
        .meta-item {
          display: flex;
          align-items: center;
          gap: 4px;
        }
      }
      
      .event-actions {
        display: flex;
        gap: 8px;
        margin-top: 10px;
        
        .ant-btn {
          flex: 1;
          border-radius: 8px;
          height: 32px;
          font-size: 12px;
          border: none;
          
          &.btn-complete {
            background: rgba(52, 199, 89, 0.1);
            color: @success-color;
            
            &:hover {
              background: rgba(52, 199, 89, 0.2);
            }
          }
          
          &.btn-edit {
            background: rgba(0, 122, 255, 0.1);
            color: @primary-color;
            
            &:hover {
              background: rgba(0, 122, 255, 0.2);
            }
          }
          
          &.btn-delete {
            background: rgba(255, 59, 48, 0.1);
            color: @danger-color;
            
            &:hover {
              background: rgba(255, 59, 48, 0.2);
            }
          }
        }
      }
      
      // 不同状态的边框颜色
      &.status-completed {
        border-left-color: @success-color;
        opacity: 0.7;
      }
      
      &.status-in-progress {
        border-left-color: @warning-color;
      }
      
      &.status-not-started {
        border-left-color: @text-secondary;
      }
    }
    
    .empty-events {
      text-align: center;
      padding: 40px 20px;
      color: @text-secondary;
      
      .anticon {
        font-size: 48px;
        color: @text-tertiary;
        margin-bottom: 12px;
      }
      
      p {
        margin: 0;
        font-size: 14px;
      }
    }
  }
}

// ========== 事项表单模态框 ==========
.event-form-modal {
  .ant-modal-content {
    border-radius: 16px;
    overflow: hidden;
  }
  
  .ant-modal-header {
    background: linear-gradient(135deg, @primary-color, @primary-light);
    border: none;
    padding: 20px 24px;
    
    .ant-modal-title {
      color: white;
      font-size: 18px;
      font-weight: 600;
    }
  }
  
  .ant-modal-body {
    padding: 24px;
  }
  
  .form-section {
    margin-bottom: 20px;
    
    &:last-child {
      margin-bottom: 0;
    }
    
    .section-title {
      font-size: 14px;
      font-weight: 600;
      color: @text-primary;
      margin-bottom: 12px;
    }
  }
  
  .ant-form-item {
    margin-bottom: 16px;
    
    .ant-form-item-label > label {
      font-weight: 500;
      color: @text-primary;
    }
    
    .ant-input,
    .ant-input-number,
    .ant-picker,
    .ant-select-selector {
      border-radius: 10px;
      border-color: @border-color;
      
      &:hover, &:focus {
        border-color: @primary-color;
      }
    }
    
    .ant-input-textarea textarea {
      border-radius: 10px;
      min-height: 100px;
    }
  }
  
  .importance-slider {
    .ant-slider-rail {
      background: @bg-secondary;
    }
    
    .ant-slider-track {
      background: linear-gradient(90deg, @success-color, @warning-color, @danger-color);
    }
    
    .ant-slider-handle {
      border-color: @primary-color;
    }
  }
  
  .color-picker-grid {
    display: grid;
    grid-template-columns: repeat(8, 1fr);
    gap: 8px;
    
    .color-option {
      width: 36px;
      height: 36px;
      border-radius: 8px;
      cursor: pointer;
      border: 2px solid transparent;
      transition: all 0.2s ease;
      
      &:hover {
        transform: scale(1.1);
      }
      
      &.selected {
        border-color: @text-primary;
        box-shadow: 0 0 0 2px white, 0 0 0 4px @text-primary;
      }
    }
  }
}

// ========== AI分析面板 ==========
.ai-analysis-panel {
  background: @bg-card;
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-radius: 16px;
  padding: 24px;
  margin-top: 20px;
  box-shadow: @shadow-lg;
  border: 1px solid @border-color;
  
  .panel-header {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 20px;
    padding-bottom: 16px;
    border-bottom: 2px solid @border-color;
    
    .ai-icon {
      width: 40px;
      height: 40px;
      border-radius: 12px;
      background: linear-gradient(135deg, @info-color, @primary-light);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 20px;
    }
    
    h3 {
      margin: 0;
      font-size: 20px;
      font-weight: 700;
      background: linear-gradient(135deg, @info-color, @primary-light);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
  }
  
  .analysis-form {
    .form-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 16px;
      margin-bottom: 16px;
      
      @media (max-width: 768px) {
        grid-template-columns: 1fr;
      }
    }
    
    .info-box {
      background: @bg-secondary;
      border-radius: 12px;
      padding: 16px;
      margin: 16px 0;
      
      .info-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 8px;
        
        &:last-child {
          margin-bottom: 0;
        }
        
        .label {
          color: @text-secondary;
          font-size: 14px;
        }
        
        .value {
          color: @text-primary;
          font-weight: 600;
          font-size: 15px;
          
          &.credits {
            color: @primary-color;
          }
          
          &.warning {
            color: @warning-color;
          }
        }
      }
    }
    
    .ant-btn-primary {
      width: 100%;
      height: 48px;
      border-radius: 12px;
      font-size: 16px;
      font-weight: 600;
      background: linear-gradient(135deg, @info-color, @primary-light);
      border: none;
      box-shadow: 0 4px 12px rgba(88, 86, 214, 0.3);
      
      &:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 16px rgba(88, 86, 214, 0.4);
      }
      
      &:disabled {
        background: @bg-secondary;
        color: @text-tertiary;
        box-shadow: none;
        transform: none;
      }
    }
  }
  
  .analysis-result {
    background: @bg-secondary;
    border-radius: 12px;
    padding: 20px;
    margin-top: 20px;
    max-height: 500px;
    overflow-y: auto;
    
    .result-content {
      color: @text-primary;
      line-height: 1.8;
      
      // Markdown渲染样式
      h2 {
        font-size: 18px;
        font-weight: 700;
        color: @primary-color;
        margin: 20px 0 12px;
        padding-bottom: 8px;
        border-bottom: 2px solid @border-color;
        
        &:first-child {
          margin-top: 0;
        }
      }
      
      h3 {
        font-size: 16px;
        font-weight: 600;
        color: @text-primary;
        margin: 16px 0 10px;
      }
      
      p {
        margin-bottom: 12px;
        color: @text-primary;
      }
      
      ul, ol {
        padding-left: 24px;
        margin-bottom: 12px;
        
        li {
          margin-bottom: 8px;
          color: @text-primary;
        }
      }
      
      table {
        width: 100%;
        border-collapse: collapse;
        margin: 16px 0;
        background: white;
        border-radius: 8px;
        overflow: hidden;
        
        th {
          background: @primary-color;
          color: white;
          padding: 12px;
          text-align: left;
          font-weight: 600;
        }
        
        td {
          padding: 10px 12px;
          border-bottom: 1px solid @border-color;
        }
        
        tr:last-child td {
          border-bottom: none;
        }
        
        tr:nth-child(even) {
          background: @bg-secondary;
        }
      }
      
      code {
        background: rgba(0, 122, 255, 0.1);
        color: @primary-color;
        padding: 2px 6px;
        border-radius: 4px;
        font-family: 'Menlo', 'Monaco', monospace;
        font-size: 0.9em;
      }
      
      blockquote {
        border-left: 4px solid @primary-color;
        padding-left: 16px;
        margin: 16px 0;
        color: @text-secondary;
        font-style: italic;
      }
    }
    
    .result-actions {
      display: flex;
      gap: 12px;
      margin-top: 16px;
      padding-top: 16px;
      border-top: 1px solid @border-color;
      
      .ant-btn {
        flex: 1;
        border-radius: 10px;
        height: 40px;
      }
    }
  }
  
  .analysis-loading {
    text-align: center;
    padding: 60px 20px;
    
    .ant-spin {
      .ant-spin-dot-item {
        background-color: @primary-color;
      }
    }
    
    p {
      margin-top: 16px;
      color: @text-secondary;
      font-size: 14px;
    }
  }
}

// ========== 响应式调整 ==========
@media (max-width: 768px) {
  .calendar-page {
    padding: 12px;
  }
  
  .calendar-header {
    padding: 16px;
    
    .header-title h1 {
      font-size: 22px;
    }
  }
  
  .calendar-main-panel {
    padding: 16px;
    
    .ant-picker-calendar {
      .ant-picker-content {
        .ant-picker-cell {
          .ant-picker-cell-inner {
            height: 60px;
            padding: 6px 2px;
          }
          
          .ant-picker-calendar-date-value {
            font-size: 14px;
          }
          
          .event-dots {
            gap: 2px;
            
            .event-dot {
              width: 4px;
              height: 4px;
            }
          }
        }
      }
    }
  }
  
  .ai-analysis-panel {
    padding: 16px;
    
    .panel-header {
      h3 {
        font-size: 18px;
      }
    }
  }
}

// ========== 滚动条美化 ==========
.calendar-right-panel,
.analysis-result {
  &::-webkit-scrollbar {
    width: 8px;
  }
  
  &::-webkit-scrollbar-track {
    background: transparent;
  }
  
  &::-webkit-scrollbar-thumb {
    background: @text-tertiary;
    border-radius: 4px;
    
    &:hover {
      background: @text-secondary;
    }
  }
}

// ========== 动画效果 ==========
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.event-card,
.stat-card {
  animation: fadeIn 0.3s ease;
}

// ========== 深色模式（可选） ==========
@media (prefers-color-scheme: dark) {
  .calendar-page {
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
  }
  
  .calendar-header,
  .calendar-left-panel,
  .calendar-main-panel,
  .calendar-right-panel,
  .ai-analysis-panel {
    background: @dark-bg-card;
    border-color: @dark-border-color;
    
    h1, h2, h3, h4, .section-title, .event-title {
      color: @dark-text-primary;
    }
    
    p, .stat-label, .category-name, .meta-item {
      color: @dark-text-secondary;
    }
  }
  
  .ant-picker-calendar {
    .ant-picker-calendar-date-value {
      color: @dark-text-primary;
    }
  }
  
  .event-card,
  .stat-card,
  .category-item {
    background: @dark-bg-secondary;
  }
}

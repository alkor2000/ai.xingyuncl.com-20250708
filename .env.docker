# Docker容器内部配置文件
# 由docker-compose.yml传入环境变量后，容器内部使用此文件连接服务

# 环境模式
NODE_ENV=production

# 应用配置
APP_NAME="AI Platform"
APP_DOMAIN=${APP_DOMAIN}
PORT=4000

# 数据库配置（Docker内部使用服务名）
DB_HOST=mysql
DB_PORT=3306
DB_USER=${DB_USER}
DB_PASSWORD=${DB_PASSWORD}
DB_NAME=${DB_NAME}

# Redis配置（Docker内部使用服务名）
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=${REDIS_PASSWORD:-}
REDIS_DB=0

# JWT配置（从外部环境变量传入）
JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET}
JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
JWT_ACCESS_EXPIRES=24h
JWT_REFRESH_EXPIRES=30d

# CORS配置
CORS_ORIGINS=${CORS_ORIGINS}

# 文件上传配置
UPLOAD_DIR=/app/storage/uploads
UPLOAD_MAX_SIZE=10485760

# 日志配置
LOG_LEVEL=info
LOG_DIR=/app/logs

# 速率限制
RATE_LIMIT_MAX=1000

# Docker环境专用配置文件
# 这个文件在Docker环境中使用，覆盖默认的.env配置

# 环境模式
NODE_ENV=production

# 应用配置
APP_NAME="AI Platform"
APP_DOMAIN=${APP_DOMAIN:-localhost}
PORT=4000

# 数据库配置（Docker内部使用服务名）
DB_HOST=mysql
DB_PORT=3306
DB_USER=ai_user
DB_PASSWORD=${MYSQL_PASSWORD:-AiPlatform@2025!}
DB_NAME=ai_platform

# Redis配置（Docker内部使用服务名）
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=${REDIS_PASSWORD:-}
REDIS_DB=0

# JWT配置（应该从外部环境变量传入）
JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET:-your-super-secret-jwt-key-change-this-in-production-2025}
JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET:-your-refresh-token-secret-change-this-2025}
JWT_ACCESS_EXPIRES=12h
JWT_REFRESH_EXPIRES=14d

# CORS配置（根据实际域名配置）
CORS_ORIGINS=${CORS_ORIGINS:-http://localhost,https://localhost}

# 文件上传配置
UPLOAD_DIR=/app/storage/uploads
UPLOAD_MAX_SIZE=10485760

# 日志配置
LOG_LEVEL=info
LOG_DIR=/app/logs

# 速率限制
RATE_LIMIT_MAX=1000

# MySQL根密码（用于初始化）
MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD:-rootpass2025}

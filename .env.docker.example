# Docker环境变量配置示例
# 复制此文件为 .env.docker 并根据实际情况修改

# 环境模式
NODE_ENV=production

# 应用配置
APP_NAME="AI Platform"
APP_DOMAIN=${APP_DOMAIN:-localhost}
PORT=4000

# 存储路径配置（Docker容器内路径）
STORAGE_PATH=/app/storage
LOG_DIR=/app/logs

# 数据库配置（Docker内部使用服务名）
DB_HOST=mysql
DB_PORT=3306
DB_USER=ai_user
DB_PASSWORD=${MYSQL_PASSWORD:-AiPlatform@2025!}
DB_NAME=ai_platform
DB_CONNECTION_LIMIT=10

# Redis配置（Docker内部使用服务名）
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=${REDIS_PASSWORD:-}
REDIS_DB=0
REDIS_KEY_PREFIX=ai_platform:

# JWT配置（必须修改为您自己的密钥！）
JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET:-your-super-secret-jwt-key-change-this-in-production}
JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET:-your-refresh-token-secret-change-this}
JWT_ACCESS_EXPIRES=24h
JWT_REFRESH_EXPIRES=30d

# CORS配置（根据实际域名配置）
CORS_ORIGINS=${CORS_ORIGINS:-http://localhost,https://localhost}

# 文件上传配置
UPLOAD_MAX_SIZE=104857600  # 100MB

# 速率限制
RATE_LIMIT_MAX=1000

# 调试选项
DEBUG_CONFIG=false

# MySQL根密码（用于初始化）
MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD:-rootpass2025}

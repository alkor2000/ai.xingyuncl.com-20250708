# 企业Docker部署配置模板
# 部署时复制此文件为 .env 并根据企业实际情况修改

# === 基础配置 ===
NODE_ENV=production
APP_NAME="您的企业AI平台名称"
APP_DOMAIN=your-domain.com
PORT=4000

# === 存储配置（Docker容器路径，无需修改） ===
STORAGE_PATH=/app/storage
LOG_DIR=/app/logs

# === 数据库配置（Docker服务名，无需修改主机名） ===
DB_HOST=mysql
DB_PORT=3306
DB_USER=ai_user
DB_PASSWORD=【请设置强密码】
DB_NAME=ai_platform
DB_CONNECTION_LIMIT=20

# === Redis配置（Docker服务名，无需修改主机名） ===
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=【建议设置密码】
REDIS_DB=0
REDIS_KEY_PREFIX=ai_platform:

# === JWT安全配置（必须修改！） ===
# 可以使用以下命令生成随机密钥：
# node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
JWT_ACCESS_SECRET=【必须生成新的随机密钥】
JWT_REFRESH_SECRET=【必须生成新的随机密钥】
JWT_ACCESS_EXPIRES=24h
JWT_REFRESH_EXPIRES=30d

# === CORS配置（修改为您的域名） ===
CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# === 其他配置 ===
UPLOAD_MAX_SIZE=104857600  # 100MB
RATE_LIMIT_MAX=1000
DEBUG_CONFIG=false

# === MySQL初始化密码 ===
MYSQL_ROOT_PASSWORD=【设置MySQL root密码】
MYSQL_PASSWORD=【与DB_PASSWORD保持一致】
